class Solution {
    public boolean isPalindrome(int x) {
        int num=x,rem=0,y=0;
        while(x>0)
        {
            rem=x%10;
            y=y*10+rem;
            x=x/10;
        }
        if(num==y)
        {
            return true;
        }
        return false;
    }
}